# Documentation i18n - Fichiers de traduction

Ce document décrit l'organisation complète des fichiers de traduction (i18n) pour la plateforme Agent PF.

## 📁 Organisation générale

La plateforme utilise deux niveaux de traduction :
1. **Traductions de la plateforme** : Pour l'interface générale (menus, navigation, paramètres, etc.)
2. **Traductions des agents** : Pour chaque agent spécifique

---

## 🌍 Langues supportées

- **Français (fr)** - Langue par défaut
- **Anglais (en)**
- **Espagnol (es)**

---

## 📂 1. Fichiers de traduction de la PLATEFORME

**Emplacement :** `/ui/frontend/src/assets/i18n/`

### Fichiers disponibles

```
/ui/frontend/src/assets/i18n/
├── fr.json  (Français)
├── en.json  (Anglais)
└── es.json  (Espagnol)
```

### Structure des clés

Les fichiers de traduction de la plateforme contiennent les sections suivantes :

| Section | Clés | Description |
|---------|------|-------------|
| `app.*` | `app.title`, `app.description` | Informations générales de l'application |
| `menu.*` | `menu.home`, `menu.agents`, `menu.settings`, etc. | Éléments de navigation et menus |
| `theme.*` | `theme.light`, `theme.dark`, `theme.colorblind`, `theme.select` | Sélection de thèmes |
| `language.*` | `language.fr`, `language.en`, `language.es`, `language.select` | Sélection de langues |
| `accessibility.*` | `accessibility.title`, `accessibility.zoom`, `accessibility.high_contrast`, etc. | Paramètres d'accessibilité |
| `auth.*` | `auth.login`, `auth.logout`, `auth.profile`, etc. | Authentification et profil utilisateur |
| `common.*` | `common.save`, `common.cancel`, `common.delete`, etc. | Actions communes réutilisables |
| `agents.*` | Descriptions et labels des agents | Informations sur les agents dans le catalogue |
| `moderation.*` | Paramètres de modération | Configuration de la modération des contenus |
| `errors.*` | Messages d'erreur | Tous les messages d'erreur de l'application |
| `validation.*` | Messages de validation | Messages de validation des formulaires |

### Exemple de clés communes

```json
{
  "menu.home": "Accueil",
  "menu.agents": "Agents",
  "menu.settings": "Paramètres",
  "theme.light": "Clair",
  "theme.dark": "Sombre",
  "theme.colorblind": "Daltonien",
  "accessibility.zoom": "Zoom",
  "accessibility.high_contrast": "Contraste élevé",
  "common.save": "Enregistrer",
  "common.cancel": "Annuler"
}
```

---

## 📂 2. Fichiers de traduction des AGENTS

Chaque agent possède ses propres fichiers de traduction dans son dossier respectif.

**Emplacement :** `/agents/{nom-de-l-agent}/i18n/`

### Liste complète des agents avec i18n

| Agent | Chemin i18n |
|-------|-------------|
| **Assistant IA** | `/agents/ai-chat-agent/i18n/` |
| **Planificateur de rendez-vous** | `/agents/appointment-scheduler-tool/i18n/` |
| **Analyse de contrats** | `/agents/contract-analysis-agent/i18n/` |
| **Analyseur de documents** | `/agents/document-analyzer-tool/i18n/` |
| **Statistiques Dolibarr** | `/agents/dolibarr-stats-agent/i18n/` |
| **Analyseur POD** | `/agents/email-pod-attachment-analyzer/i18n/` |
| **Surveillance web** | `/agents/web-monitoring-agent/i18n/` |

### Structure type pour chaque agent

Chaque agent contient **exactement 3 fichiers** :

```
/agents/{nom-agent}/i18n/
├── fr.json
├── en.json
└── es.json
```

### Exemple : Assistant IA (`ai-chat-agent`)

```
/agents/ai-chat-agent/i18n/
├── fr.json  ← Traductions françaises de l'agent
├── en.json  ← Traductions anglaises de l'agent
└── es.json  ← Traductions espagnoles de l'agent
```

### Contenu type d'un fichier agent

Les fichiers de traduction des agents contiennent généralement :
- Titre et description de l'agent
- Labels des champs de formulaire
- Messages de statut
- Messages d'erreur spécifiques
- Textes de l'interface de l'agent
- Tooltips et aide contextuelle

```json
{
  "title": "Assistant IA Professionnel",
  "description": "Interface de chat IA avec modération",
  "placeholder": "Tapez votre message...",
  "send": "Envoyer",
  "clear": "Effacer",
  "config": "Configuration",
  "messages": {
    "you": "Vous",
    "assistant": "Assistant",
    "count": "messages",
    "error": "Erreur lors de l'envoi"
  }
}
```

---

## 🔧 Comment ajouter/modifier des traductions

### Pour la PLATEFORME

1. Ouvrir le fichier de la langue souhaitée dans `/ui/frontend/src/assets/i18n/`
2. Ajouter ou modifier la clé dans la section appropriée
3. **IMPORTANT :** Répéter l'opération pour les 3 langues (fr, en, es)

**Exemple :**
```json
// Dans fr.json
"menu.new_feature": "Nouvelle fonctionnalité"

// Dans en.json
"menu.new_feature": "New Feature"

// Dans es.json
"menu.new_feature": "Nueva Funcionalidad"
```

### Pour un AGENT

1. Identifier le dossier de l'agent : `/agents/{nom-agent}/i18n/`
2. Modifier le fichier de la langue souhaitée
3. **IMPORTANT :** Répéter l'opération pour les 3 langues (fr, en, es)

---

## ✅ Bonnes pratiques

1. **Toujours maintenir les 3 langues à jour** - Ne jamais ajouter une traduction dans une seule langue
2. **Utiliser des clés descriptives** - `menu.home` plutôt que `m1`
3. **Organiser par section** - Regrouper les clés liées (menu.*, auth.*, etc.)
4. **Éviter les textes hardcodés** - Toujours utiliser les clés de traduction dans le code
5. **Tester dans toutes les langues** - Vérifier que les traductions s'affichent correctement
6. **Respecter la casse** - Adapter la casse selon la langue (majuscules en allemand, etc.)

---

## 🔍 Debugging i18n

### Vérifier qu'une traduction existe

1. Rechercher la clé dans les fichiers `/ui/frontend/src/assets/i18n/*.json`
2. Pour un agent, chercher dans `/agents/{nom-agent}/i18n/*.json`

### Traduction manquante

Si une clé n'est pas traduite :
- L'application affichera la clé elle-même (ex: `menu.missing_key`)
- Vérifier dans la console du navigateur les warnings de traduction manquante

### Recharger les traductions

Après modification des fichiers :
1. Redémarrer le serveur de développement Angular
2. Vider le cache du navigateur (Ctrl+Shift+R)
3. Vérifier que le fichier JSON est valide (pas d'erreur de syntaxe)

---

## 📊 Statistiques

- **Fichiers de plateforme :** 3 (fr, en, es)
- **Nombre d'agents avec i18n :** 7
- **Total de fichiers de traduction agents :** 21 (7 agents × 3 langues)
- **Total général :** 24 fichiers de traduction

---

## 🎯 Référence rapide

### Je veux traduire...

| Quoi ? | Où ? |
|--------|------|
| Un menu de navigation | `/ui/frontend/src/assets/i18n/{lang}.json` → section `menu.*` |
| Un message d'erreur global | `/ui/frontend/src/assets/i18n/{lang}.json` → section `errors.*` |
| Un bouton commun (Enregistrer, Annuler) | `/ui/frontend/src/assets/i18n/{lang}.json` → section `common.*` |
| Le titre d'un agent | `/agents/{nom-agent}/i18n/{lang}.json` |
| Un message d'un agent spécifique | `/agents/{nom-agent}/i18n/{lang}.json` |
| Les paramètres d'accessibilité | `/ui/frontend/src/assets/i18n/{lang}.json` → section `accessibility.*` |
| Les thèmes (clair/sombre) | `/ui/frontend/src/assets/i18n/{lang}.json` → section `theme.*` |

---

## 📝 Notes importantes

- Les fichiers JSON doivent être **valides** (pas de virgules en trop, guillemets corrects)
- Les clés sont **sensibles à la casse** : `menu.Home` ≠ `menu.home`
- Ne jamais supprimer une clé sans vérifier qu'elle n'est plus utilisée dans le code
- Pour les pluriels complexes, consulter la documentation de ngx-translate

---

**Dernière mise à jour :** 2026-01-11
